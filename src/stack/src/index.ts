// todo: filtering internals
// at(fn) to get the call site at that function call
// stack([limit], [fn]) to get the stack from the function
// fromError(er: Error) to convert a stack from an error object
import { CallSiteLike } from './call-site-like.js'

export { CallSiteLike }
